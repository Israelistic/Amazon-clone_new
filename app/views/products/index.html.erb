<h1> Amazon Rainforest </h1>

<h4><%= link_to 'Add New Product', new_product_url  %></h4>

<% @products.each do |product| %>
<p><%= link_to(image_tag(product.url), product_url(product))%></p>
<p><%= product.price_in_cents %></p>
<% end %>
